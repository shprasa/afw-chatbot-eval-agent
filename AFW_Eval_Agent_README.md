# AFW Screening Chatbot Evaluation Agent — quick reference

All outputs are saved under `workspace/` in this repo.

```powershell
pip install -r requirements.txt
python run_eval_agent.py
```

See **README.md** for full step-by-step instructions.
